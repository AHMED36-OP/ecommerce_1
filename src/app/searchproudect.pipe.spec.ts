import { SearchproudectPipe } from './searchproudect.pipe';

describe('SearchproudectPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchproudectPipe();
    expect(pipe).toBeTruthy();
  });
});
